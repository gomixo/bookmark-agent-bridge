# Bookmark Agent Bridge

本上下文定义 Agent 通过 Chrome 原生书签能力执行整理任务时使用的核心语言。

## Language

**书签工具层**：
向 Agent 暴露 Chrome 原生书签查询和修改能力的薄层，不负责生成整理策略。
_Avoid_: AI 书签管理器、书签整理器

**扩展实例**：
安装在某个 Chrome Profile 中并操作该 Profile 书签树的扩展运行实例。
_Avoid_: 用户账号、Profile 路由

**临时桥接服务**：
仅在一次 Agent 书签任务期间运行，转发 CLI 请求与扩展响应的 localhost WebSocket 进程。
_Avoid_: Windows 服务、后台守护进程、常驻服务

**书签任务**：
Agent 对一个扩展实例执行的一段有明确开始和结束的书签操作过程。
_Avoid_: 会话同步、后台整理

**能力开关**：
由用户在扩展中持久设置的写入或删除授权边界。
_Avoid_: Chrome 权限、逐项确认

**实时节点 ID**：
扩展从当前书签树读取并在本次任务中返回的 Chrome 书签节点标识。
_Avoid_: 永久书签 ID、URL 主键

**批处理**：
按顺序执行多个书签操作并逐项返回结果的请求；它不提供事务保证。
_Avoid_: 事务、原子计划

**重复组**：
按完全相同 URL 或保守标准化 URL 聚合的一组书签节点。
_Avoid_: 可安全删除项、相似网页

**整理计划**：
Agent 根据实时书签树生成的操作集合，不由扩展存储或编辑。
_Avoid_: 扩展规则、自动分类配置
